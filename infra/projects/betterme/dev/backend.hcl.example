# Copy to backend.hcl for local init (gitignored). CI passes these via workflow env vars.
resource_group_name  = "rg-betterme-tfstate-sea"
storage_account_name = "stbettermetfstateae"
container_name       = "dev"
key                  = "projects-betterme-dev.tfstate"
